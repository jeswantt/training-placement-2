class Solution(object):
    def sortByBits(self, arr):
        """
        :type arr: List[int]
        :rtype: List[int]
        """
        
        result=sorted(arr, key=lambda x:(bin(x).count('1'),x))
        return result
        
