class Solution(object):
    def lengthOfLastWord(self, s):
        s=s.strip()
        i=len(s)-1
        while i>=0:
            if s[i]==' ':
                return len(s)-i-1
            else:
                i=i-1
        return len(s)
